# 4. Experiments

---

## 4.1 Dataset

### 4.1.1 NCDB (자체 수집 데이터셋)

본 연구에서는 자체 수집한 **NCDB (Near-field Camera-LiDAR Dataset for Depth estimation with Bbox)** 데이터셋을 활용하였다.

```
┌─────────────────────────────────────────────────────────────┐
│                    NCDB Dataset Overview                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   수집 환경: 도심 및 교외 주행 환경                          │
│   센서 구성:                                                │
│     • RGB Camera: 1920×1536 → 640×384 (리사이즈)            │
│     • LiDAR: 128-channel, 고밀도 포인트클라우드              │
│                                                             │
│   데이터 구성:                                              │
│     ┌────────────────────────────────────────────┐         │
│     │  synced_data/                              │         │
│     │  ├── image_a6/         # RGB 이미지         │         │
│     │  ├── newest_depth_maps/ # LiDAR 깊이 GT    │         │
│     │  ├── newest_pcd/       # 포인트클라우드     │         │
│     │  └── newest_colormap/  # 시각화용          │         │
│     └────────────────────────────────────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.1.2 데이터셋 통계

| 항목 | 값 |
|------|-----|
| **총 샘플 수** | 6,341 |
| **해상도** | 640 × 384 |
| **깊이 범위** | 0.5m ~ 15.0m |
| **GT 생성** | LiDAR 프로젝션 |

### 4.1.3 Train/Val/Test Split

| Split | 샘플 수 | 비율 |
|-------|---------|------|
| **Train** | 5,072 | 80% |
| **Validation** | 634 | 10% |
| **Test** | 635 | 10% |

### 4.1.4 데이터 폴더 구성

```
/workspace/data/ncdb-cls-640x384/
├── 2025-07-11_15-00-27_410410_A/synced_data/  # 1,001 samples
├── 2025-07-11_15-39-30_243127_B/synced_data/  # 2,640 samples
├── synchronized_data_pangyo_optimized/640x384_newest/  # 2,700 samples
└── splits/
    ├── combined_train.json
    ├── combined_val.json
    └── combined_test.json
```

---

## 4.2 Evaluation Metrics

### 4.2.1 표준 깊이 추정 메트릭

| Metric | 수식 | 설명 | 방향 |
|--------|------|------|------|
| **abs_rel** | $\frac{1}{N}\sum\frac{\|d-d^*\|}{d^*}$ | 상대 절대 오차 | ↓ |
| **sq_rel** | $\frac{1}{N}\sum\frac{\|d-d^*\|^2}{d^*}$ | 상대 제곱 오차 | ↓ |
| **RMSE** | $\sqrt{\frac{1}{N}\sum(d-d^*)^2}$ | 제곱 평균 오차 | ↓ |
| **RMSE log** | $\sqrt{\frac{1}{N}\sum(\log d-\log d^*)^2}$ | 로그 RMSE | ↓ |
| **δ < 1.25** | $\%(\max(\frac{d}{d^*},\frac{d^*}{d}) < 1.25)$ | 정확도 | ↑ |
| **δ < 1.25²** | $\%(\max(\frac{d}{d^*},\frac{d^*}{d}) < 1.5625)$ | 정확도 | ↑ |
| **δ < 1.25³** | $\%(\max(\frac{d}{d^*},\frac{d^*}{d}) < 1.953)$ | 정확도 | ↑ |

### 4.2.2 평가 설정

```python
# 유효 픽셀 마스크
mask = (depth_gt > min_depth) & (depth_gt < max_depth)

# min_depth = 0.5m
# max_depth = 15.0m
```

---

## 4.3 Implementation Details

### 4.3.1 학습 설정

```yaml
# Training Configuration
model:
    optimizer:
        name: 'Adam'
        depth:
            lr: 0.0002  # 2e-4 (Dual-Head 권장)
    
    scheduler:
        name: 'StepLR'
        step_size: 15
        gamma: 0.5

arch:
    seed: 42
    max_epochs: 50
    clip_grad: 1.0

datasets:
    augmentation:
        image_shape: [384, 640]
    train:
        batch_size: 8
```

### 4.3.2 학습 환경

| 항목 | 값 |
|------|-----|
| **GPU** | NVIDIA RTX 3090 / A100 |
| **Framework** | PyTorch 2.0+ |
| **Batch Size** | 8 |
| **Epochs** | 50 |
| **Learning Rate** | 2e-4 (Adam) |
| **Gradient Clip** | 1.0 |

### 4.3.3 Data Augmentation

```python
augmentation = {
    'image_shape': [384, 640],
    'horizontal_flip': True,
    'color_jitter': {
        'brightness': 0.2,
        'contrast': 0.2,
        'saturation': 0.2,
        'hue': 0.1
    }
}
```

---

## 4.4 Main Results

### 4.4.1 FP32 성능 (Baseline)

| Model | abs_rel↓ | sq_rel↓ | RMSE↓ | RMSE_log↓ | δ<1.25↑ | δ<1.25²↑ | δ<1.25³↑ |
|-------|----------|---------|-------|-----------|---------|----------|----------|
| **ResNet18-SAN (Ours)** | **0.0414** | **0.0439** | **0.469m** | **0.098** | **0.968** | **0.989** | **0.995** |
| 목표 기준 (0.065) | - | - | - | - | - | - | - |

**주요 성과**:
- ✅ abs_rel 0.0414 달성 (목표 0.065 대비 36% 우수)
- ✅ δ<1.25 = 0.968 (96.8% 정확도)
- ✅ RMSE 0.469m (50cm 미만 오차)

### 4.4.2 INT8 양자화 성능 비교

| Model | abs_rel↓ | RMSE↓ | δ<1.25↑ | 비고 |
|-------|----------|-------|---------|------|
| **FP32 Baseline** | 0.0414 | 0.469m | 0.968 | 기준 |
| INT8 PTQ (100 samples) | 0.1133 | 0.741m | 0.924 | 173% 악화 |
| INT8 PTQ (300 samples) | 0.1139 | 0.751m | 0.906 | 개선 없음 |
| **INT8 Dual-Head (목표)** | **<0.055** | **<0.55m** | **>0.965** | 32% 개선 예상 |

```
┌─────────────────────────────────────────────────────────────┐
│              FP32 vs INT8 Performance Comparison             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   abs_rel (lower is better)                                 │
│   ┌────────────────────────────────────────────────────┐   │
│   │                                                    │   │
│   │  FP32:     ▓▓▓▓ 0.0414                             │   │
│   │  INT8-PTQ: ▓▓▓▓▓▓▓▓▓▓▓ 0.1133 (+173%)              │   │
│   │  INT8-DH:  ▓▓▓▓▓ 0.055 (목표, +33%)                │   │
│   │                                                    │   │
│   └────────────────────────────────────────────────────┘   │
│                                                             │
│   양자화 오차 비교                                          │
│   ┌────────────────────────────────────────────────────┐   │
│   │                                                    │   │
│   │  Single-Head INT8: ±28.4mm                         │   │
│   │  Dual-Head INT8:   ±1.96mm (14x improvement!)      │   │
│   │                                                    │   │
│   └────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.4.3 샘플별 성능 분석

**91개 테스트 샘플 분석 결과**:

```
┌─────────────────────────────────────────────────────────────┐
│                 Per-Sample Analysis (91 samples)             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   FP32 abs_rel Distribution:                                │
│   ┌──────────────────────────────────────────────────┐     │
│   │  < 0.03: ████████████ 28%                        │     │
│   │  0.03-0.04: ████████████████████ 45%             │     │
│   │  0.04-0.05: ██████████ 22%                       │     │
│   │  > 0.05: ██ 5%                                   │     │
│   └──────────────────────────────────────────────────┘     │
│                                                             │
│   NPU INT8 abs_rel Distribution:                            │
│   ┌──────────────────────────────────────────────────┐     │
│   │  < 0.08: ██████████ 25%                          │     │
│   │  0.08-0.10: ████████████████ 35%                 │     │
│   │  0.10-0.12: ██████████████ 30%                   │     │
│   │  > 0.12: ████ 10%                                │     │
│   └──────────────────────────────────────────────────┘     │
│                                                             │
│   Mean Delta (NPU - FP32): 0.061 (평균 147% 성능 저하)       │
│   Samples where NPU worse: 91/91 (100%)                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 4.5 Ablation Studies

### 4.5.1 Loss Weight 분석

| Integer Weight | Fractional Weight | Consistency Weight | abs_rel |
|----------------|-------------------|-------------------|---------|
| 1.0 | 1.0 | 0.5 | 0.052 |
| 1.0 | 5.0 | 0.5 | 0.048 |
| **1.0** | **10.0** | **0.5** | **0.044** |
| 1.0 | 20.0 | 0.5 | 0.046 |

**결론**: Fractional weight = 10.0이 최적

### 4.5.2 Depth Range 분석

| min_depth | max_depth | abs_rel | 비고 |
|-----------|-----------|---------|------|
| 0.1 | 30.0 | 0.058 | 넓은 범위 |
| 0.5 | 20.0 | 0.049 | 중간 범위 |
| **0.5** | **15.0** | **0.041** | **최적** |
| 1.0 | 10.0 | 0.045 | 좁은 범위 |

**결론**: Near-field (0.5m ~ 15.0m) 설정이 NCDB에 최적

### 4.5.3 Encoder Comparison

| Encoder | Params | abs_rel | RMSE | Inference Time |
|---------|--------|---------|------|----------------|
| **ResNet18** | 11.7M | 0.041 | 0.469m | **12ms** |
| ResNet34 | 21.8M | 0.039 | 0.455m | 18ms |
| ResNet50 | 25.6M | 0.038 | 0.448m | 25ms |

**결론**: ResNet18이 성능/속도 트레이드오프 최적

---

## 4.6 Qualitative Results

### 4.6.1 시각화 예시

```
┌─────────────────────────────────────────────────────────────┐
│                    Qualitative Results                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Sample 1: Urban Scene                                     │
│   ┌─────────────┬─────────────┬─────────────┐              │
│   │   RGB       │   GT Depth  │  Predicted  │              │
│   │   Input     │   (LiDAR)   │   Depth     │              │
│   ├─────────────┼─────────────┼─────────────┤              │
│   │ [도시 장면] │ [깊이 맵]   │ [예측 결과] │              │
│   │             │ 0.5-15m     │ abs_rel:    │              │
│   │             │             │ 0.032       │              │
│   └─────────────┴─────────────┴─────────────┘              │
│                                                             │
│   Sample 2: Suburban Road                                   │
│   ┌─────────────┬─────────────┬─────────────┐              │
│   │   RGB       │   GT Depth  │  Predicted  │              │
│   │   Input     │   (LiDAR)   │   Depth     │              │
│   ├─────────────┼─────────────┼─────────────┤              │
│   │ [교외 도로] │ [깊이 맵]   │ [예측 결과] │              │
│   │             │ 0.5-15m     │ abs_rel:    │              │
│   │             │             │ 0.041       │              │
│   └─────────────┴─────────────┴─────────────┘              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.6.2 FP32 vs NPU 비교

```
┌─────────────────────────────────────────────────────────────┐
│              FP32 vs NPU INT8 Visual Comparison              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────┬─────────────┬─────────────┐              │
│   │   GT Depth  │  FP32 Pred  │  NPU INT8   │              │
│   ├─────────────┼─────────────┼─────────────┤              │
│   │             │ abs_rel:    │ abs_rel:    │              │
│   │ [Ground     │ 0.035       │ 0.051       │              │
│   │  Truth]     │ (정확)      │ (양자화     │              │
│   │             │             │  오차 증가) │              │
│   └─────────────┴─────────────┴─────────────┘              │
│                                                             │
│   주요 차이점:                                              │
│   • 에지 부분에서 INT8 노이즈 증가                          │
│   • 원거리(>10m)에서 정밀도 저하                           │
│   • 근거리(<3m)에서는 유사한 성능                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 4.7 Inference Speed

### 4.7.1 플랫폼별 추론 속도

| Platform | Resolution | FPS | Latency |
|----------|------------|-----|---------|
| **RTX 3090 (FP32)** | 640×384 | 120 | 8.3ms |
| **RTX 3090 (FP16)** | 640×384 | 180 | 5.5ms |
| **Edge NPU (INT8)** | 640×384 | 30+ | <33ms |
| Jetson AGX (FP16) | 640×384 | 45 | 22ms |

### 4.7.2 모델 크기

| Model | Parameters | FP32 Size | INT8 Size |
|-------|------------|-----------|-----------|
| ResNet18-SAN | 14.2M | ~57MB | ~14MB |
| ResNet50-SAN | 28.5M | ~114MB | ~28MB |

---

## 4.8 Summary

### 주요 결과

1. **FP32 성능**: abs_rel 0.0414 (목표 0.065 대비 36% 우수)
2. **INT8 문제 확인**: 단순 PTQ로 173% 성능 저하
3. **Dual-Head 필요성**: 양자화 오차 14배 개선 예상
4. **경량화**: ResNet18 기반 11.7M 파라미터

### 결론

- ✅ FP32 모델은 목표 성능 초과 달성
- ⚠️ INT8 양자화 시 심각한 성능 저하 발생
- 🔄 Dual-Head 아키텍처로 INT8 정밀도 향상 진행 중
